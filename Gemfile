source "https://rubygems.org"

# GitHub Pages bundle (includes Jekyll, jekyll-remote-theme,
# and the plugins used by github.io builds).
gem "github-pages", group: :jekyll_plugins

# Required by this site but not bundled in github-pages.
gem "jekyll-include-cache", group: :jekyll_plugins
